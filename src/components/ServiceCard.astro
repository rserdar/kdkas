---
interface Props {
  icon: string;
  title: string;
  description: string;
  href?: string;
  delay?: number;
}
const { icon, title, description, href, delay = 0 } = Astro.props;
const Tag = href ? 'a' : 'div';
---

<Tag
  href={href}
  class="group relative flex flex-col overflow-hidden rounded-2xl border border-slate-100 bg-white shadow-sm transition-all duration-300 hover:-translate-y-1 hover:shadow-xl dark:border-slate-800 dark:bg-slate-900"
  data-animate="fadeInUp"
  data-delay={delay}
>
  <!-- Kırmızı üst çizgi -->
  <div class="h-[3px] w-full bg-gradient-to-r from-primary/40 via-primary to-primary/40
              scale-x-0 group-hover:scale-x-100 transition-transform duration-300 origin-left"></div>

  <div class="flex flex-col flex-1 p-7">
    <!-- İkon -->
    <div class="mb-6 w-14 h-14 rounded-xl bg-primary/10 flex items-center justify-center text-primary
                group-hover:bg-primary group-hover:text-white transition-all duration-300 shrink-0"
         set:html={icon}>
    </div>

    <!-- Başlık -->
    <h3 class="mb-3 font-poppins text-xl font-bold text-dark transition-colors duration-200 group-hover:text-primary">
      {title}
    </h3>

    <!-- Açıklama -->
    <p class="flex-1 text-[15px] leading-relaxed text-slate-500 dark:text-slate-300">
      {description}
    </p>

    <!-- Link -->
    {href && (
      <div class="mt-6 flex items-center gap-2 border-t border-slate-100 pt-5 text-sm font-semibold text-slate-400 transition-colors duration-200 group-hover:text-primary dark:border-slate-800 dark:text-slate-400">
        Detaylı Bilgi
        <svg class="w-4 h-4 transform group-hover:translate-x-1 transition-transform duration-200"
             fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3" />
        </svg>
      </div>
    )}
  </div>
</Tag>
